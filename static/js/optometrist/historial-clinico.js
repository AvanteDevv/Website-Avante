/* =========================================================
   HISTORIAL CLÍNICO — vista sin backend todavía. El buscador ya
   está deshabilitado desde el HTML (atributo disabled); este
   archivo solo se encarga de los íconos.
   ========================================================= */
if (window.feather) feather.replace();