/* =========================================================
   EXAMEN DE LA VISTA — vista sin backend todavía. El botón
   "Nuevo examen" ya está deshabilitado desde el HTML (atributo
   disabled), así que no hace falta ningún handler de click —
   este archivo solo se encarga de los íconos.
   ========================================================= */
if (window.feather) feather.replace();